from namemcpy import namepy
example = namepy()
test = example.verifyLikeUsername(server='example.com', username='example')
print(test)

# OUTPUT
#if username is not liking the server then
#print(the username (username) is not liking the server (server))
#returns false
#if username is liking the server then
#print(the username (username) is liking the server (server))
#return true
